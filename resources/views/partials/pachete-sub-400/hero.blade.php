{{-- Pachete sub 400 lei — hero + notă de onestitate + chip-uri filtru. --}}
<section class="filt-hero">
  <div class="inner">
    <div class="eyebrow">{{ __('Filtru preț · Quick link', 'sage') }}</div>
    <h1>{{ __('Pachete', 'sage') }} <em>{{ __('sub 400 lei.', 'sage') }}</em></h1>
    <p class="lede"><strong>{{ __('4 pachete', 'sage') }}</strong> {{ __('de 2 suplimente, pentru o singură zonă de probleme. De la', 'sage') }} <strong>{{ __('283 lei', 'sage') }}</strong>.</p>
    <div class="honest-note">
      <strong>{{ __('Notă de onestitate', 'sage') }}</strong>
      {{ __('Am înlocuit termenul „pachete economice" cu „pachete sub 400 lei" pentru că un produs de 283 lei nu este economic. Este accesibil pentru ce oferă — dar onestitatea contează mai mult decât marketing-ul.', 'sage') }}
    </div>
    <div class="filter-chips">
      <span class="fc active">{{ __('Toate', 'sage') }} <span class="ct">4</span></span>
      <span class="fc">{{ __('Vegan', 'sage') }} <span class="ct">4 / 4</span></span>
      <span class="fc">{{ __('Cură 120 zile', 'sage') }} <span class="ct">2</span></span>
      <span class="fc">{{ __('Cură scurtă 33–50 zile', 'sage') }} <span class="ct">2</span></span>
    </div>
  </div>
</section>
